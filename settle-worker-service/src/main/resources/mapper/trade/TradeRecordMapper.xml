<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuxx.exchangeclear.trade.mapper.TradeRecordMapper">

    <select id="selectNextPage" resultType="com.wuxx.exchangeclear.trade.entity.TradeRecord">
        SELECT
            id,
            settle_date,
            member_id,
            trade_no,
            product_id,
            contract_id,
            direction,
            price,
            volume,
            amount,
            trade_time,
            created_at
        FROM trade_record
        WHERE settle_date = #{settleDate}
          AND member_id = #{memberId}
          AND id &gt; #{lastId}
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

</mapper>
